# Stream 8b — Visual Regression + Pinch-Zoom e2e + v0.1.0 Tag

Paste the block below into a fresh Claude Code session. Self-contained — do not edit before pasting.

```
You are Stream 8b of an 8-stream parallel build. Streams 1–7 all merged; you're the finale. This project is Tinker (the Brilliant-class learning app at learntinker.com). svelte-mafs is the Svelte math-viz widget engine INSIDE Tinker, not a standalone library. There is no npm publish — we tag v0.1.0 on the repo only.

READ FIRST:
- /Users/joshhd/Documents/tinker/docs/plans/2026-04-23-svelte-mafs.md (master plan)
- /Users/joshhd/Documents/tinker/docs/plans/2026-04-23-svelte-mafs-streams.md (workflow + ownership matrix)
- The MEMORY.md note on "Tinker is one project" — /Users/joshhd/.claude/projects/-Users-joshhd-Documents-tinker/memory/MEMORY.md

CURRENT STATE (main at 14b03c2):
- 643 unit tests passing across vec/math/sampling/gestures/display/interaction/theme
- Site live at https://learntinker.com/ (Tinker waitlist landing, deploys via `wrangler deploy` from apps/docs/)
- Stream 8a shipped CI (.github/workflows/ci.yml), Release workflow (release.yml), Changesets config, playwright.config.ts, tests/e2e/ skeleton, release-check.mjs
- e2e job in CI is feature-gated: only runs when BOTH apps/docs/ exists AND packages/svelte-mafs/tests/e2e/specs/*.spec.ts exists. Your first spec flips the second gate.
- There is an orphaned PR #5 (chore(release): version packages) from the changesets action. Close it — we are NOT publishing to npm this round.

SETUP:
Invoke superpowers:using-git-worktrees to create worktree at
  /Users/joshhd/Documents/tinker-worktrees/stream-8b-finale
on branch `stream/8b-finale` based on latest `main`.

SCOPE (exclusive):
- packages/svelte-mafs/tests/e2e/specs/**       ← visual regression + pinch-zoom specs live here
- packages/svelte-mafs/tests/e2e/fixtures/**    ← you may add Astro pages-under-test here IF NEEDED
- packages/svelte-mafs/tests/e2e/__screenshots__/**  ← baseline PNGs, committed
- README.md  ← rewrite for Tinker framing (was originally library-framed)
- CONTRIBUTING.md  ← add the .svelte.ts resolver trap + flushSync test helper notes
- .github/workflows/ci.yml  ← only if you need a small tweak to the e2e job config
- scripts/release-check.mjs  ← update to match current `files:` / exports reality

DO NOT TOUCH:
- packages/svelte-mafs/src/**  (lib code — do not edit)
- apps/docs/src/**  (Tinker landing — do not edit content)
- .changeset/**  (closing PR #5 means we're deferring npm)
- wrangler.jsonc  (deploy config is right)

TASKS:

1. **Tinker-flavored fixture pages for visual regression.**
   Under `packages/svelte-mafs/tests/e2e/fixtures/` add a small Astro sub-app OR inline HTML pages that exercise each major component in isolation. Alternative: point Playwright at the existing `apps/docs/src/pages/examples/stream-5-preview.astro` and add more example pages under `apps/docs/src/pages/examples/`.
   Pick ONE approach — document why in your PR. Rule of thumb: if you add pages under apps/docs/src/pages/examples/, that's a light exception to "don't touch apps/docs/src" — it's acceptable because these pages exist to back e2e tests, but you MUST keep them visually simple and label them clearly as test fixtures. Do NOT touch the Tinker landing (index.astro), nav, or layout.
   
   Pages to cover (one per component/behavior, minimal layout, full viewBox):
   - point, line-segment, line-through-points, circle, ellipse, polygon, vector
   - coordinates-cartesian (axes + grid)
   - plot-of-x (sin, tan, 1/x pathologies)
   - plot-of-y, plot-parametric, plot-vector-field, plot-inequality
   - text (KaTeX label)
   - transform (rotation, scale composition)
   - movablepoint (static initial render — drag behavior goes in a separate drag spec)

2. **Visual regression specs.**
   One `.spec.ts` per fixture page under `packages/svelte-mafs/tests/e2e/specs/visual/`. Each test: navigate to fixture, wait for `network idle`, `expect(page).toHaveScreenshot()`. Baselines go in `tests/e2e/__screenshots__/` committed to the repo. Use Playwright's default 0.2 pixel-ratio threshold → tune per-test if a particular component legitimately renders non-deterministically (animations, random vector-field seeds).

3. **Interactive drag spec (MovablePoint).**
   `specs/interaction/movable-point.spec.ts`: navigate to a movable-point fixture, perform real pointer drag from (startX,startY) to (endX,endY), assert the point's rendered SVG attributes changed to match the expected user-space coordinates (use pxToUser math from the viewBox). Also assert keyboard navigation: focus point, arrow key moves it 1 unit, Shift+arrow moves 10.

4. **Pinch-zoom e2e (Stream 3 handoff).**
   `specs/interaction/pan-zoom-pinch.spec.ts`: inject two coordinated pointer sequences into a panZoom fixture to simulate a pinch gesture. Playwright's CDPSession gives you multi-pointer control (`Input.dispatchTouchEvent` with two finger objects). Verify `onZoom` fires with a factor > 1 when fingers spread and < 1 when they pinch in, centered on the midpoint. Remove the TODO comment in `packages/svelte-mafs/src/gestures/pan-zoom.ts` once the spec is stable — ONE-LINE EDIT permitted here as part of handoff closure.

5. **README rewrite (Tinker framing).**
   Current README reads as library documentation. Replace with Tinker-first content:
   - What Tinker is (one paragraph — rigorous visual learning for adults)
   - How this repo is structured (packages/svelte-mafs = widget engine, apps/docs = the learntinker.com site)
   - Status: in development, not yet accepting PRs
   - Credit Mafs (MIT) for the widget engine design inspiration, KaTeX for typesetting
   - Link to the deployed site and the research-report plan doc

6. **CONTRIBUTING update.**
   Add two notes that came out of Streams 3 and 6:
   - The `.svelte.ts` + `.svelte` resolver trap (Svelte 5 + TypeScript bundler resolver; same-basename .ts wins silently). Rule: put rune-aware helpers in a distinct-basename file (e.g. matrix.ts, plot-utils.ts).
   - The `flushSync()` test helper pattern for forcing Svelte 5 `$effect` to run synchronously in jsdom. One-line snippet is enough.

7. **release-check.mjs update.**
   Currently flags missing dist/styles/core.css + font.css as errors. Those now exist (Stream 6 shipped them). Run the script, make sure it exits 0 on a fresh build. If it doesn't, fix the check to match reality — DO NOT add new blocking checks beyond what's already there.

8. **Close orphaned changesets PR #5 and retire the npm path.**
   We are not publishing to npm. Actions:
   - `gh pr close 5 --comment "Deferred: Tinker is a product, not a library. No npm publish for v0.1.0."`
   - Delete `.github/workflows/release.yml` — it references a non-existent NPM_TOKEN and runs changesets/action on every push, noisy.
   - Leave `.changeset/` in place for now (useful internal changelog tool) but note in CONTRIBUTING that it's internal-only.

9. **Tag v0.1.0 on main after merge.**
   Captain will do this after merging your PR. Don't tag in your branch.

DONE CRITERIA:
- `pnpm -F svelte-mafs test` green (unchanged: 643 tests)
- `pnpm -F svelte-mafs test:e2e` green with baselines committed
- All fixtures render — smoke-test with `pnpm -F docs dev` + browse each fixture URL
- CI on your PR branch: test job green, e2e job green (this is the moment the feature-gate flips for the first time — watch it carefully)
- `node scripts/release-check.mjs` exits 0
- README renders correctly on GitHub (preview in browser)
- PR opened: "Stream 8b: visual regression + pinch-zoom + Tinker framing"

COMMIT PREFIX: `test(e2e,s8b):` or `docs(s8b):` or `chore(s8b):`

Invoke superpowers:executing-plans. Begin.
```

---

## Captain notes (not part of the prompt)

- After Stream 8b merges, tag `v0.1.0` on `main` with a real release note summarizing what shipped across all 8 streams.
- Clean up the remaining worktrees (`tinker-worktrees/stream-*`) once their branches merge.
- Consider renaming the GitHub repo from `svelte-mafs` to `tinker` / `learntinker` as separate follow-up — low-risk now that all streams are landed.
