---
/**
 * /dev/widget-lab/demos/ParamBudgetPie/ — single-widget audit surface.
 * Auto-generated by scripts in plan-eng-review Phase 1.
 * Phase 1 captures a Playwright snapshot of each of these pages
 * into audit-capture/ at desktop / tablet / mobile viewports.
 */
export const prerender = true;
import Base from '../../../../layouts/Base.astro';
import ParamBudgetPie from '../../../../components/demos/ParamBudgetPie.svelte';
---
<Base title="ParamBudgetPie · Widget Lab" description="Audit surface for ParamBudgetPie" fullWidth>
  <header class="lab-bar">
    <h1>ParamBudgetPie</h1>
    <p class="lab-meta"><a href="/dev/widget-lab/">← back to index</a> · checked against the Widget Visual Quality Bar in DESIGN.md</p>
  </header>
  <div class="audit-cell" data-widget="ParamBudgetPie">
    <ParamBudgetPie client:visible />
  </div>
</Base>
<style>
  .lab-bar {
    padding: 1rem 1.5rem;
    border-bottom: 1px solid color-mix(in srgb, var(--site-fg) 12%, transparent);
  }
  h1 {
    font-family: var(--font-display);
    font-size: 1.4rem;
    margin: 0 0 0.3rem;
  }
  .lab-meta {
    font-family: var(--font-mono);
    font-size: 0.78rem;
    color: var(--site-fg-muted);
    margin: 0.2rem 0;
  }
  .lab-meta a { color: var(--ink-red); text-decoration: none; }
  .lab-meta a:hover { text-decoration: underline; }
  .audit-cell {
    padding: 1.5rem;
    max-width: 1280px;
    margin: 0 auto;
  }
</style>
